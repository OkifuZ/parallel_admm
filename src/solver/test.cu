//#include <cstdio>
//#include <cuda_runtime.h>
//

//__global__ void fill_sin(int* arr, int n) {
//    int i = blockIdx.x * blockDim.x + threadIdx.x;
//    if (i < n) return;
//    arr[i] = sinf(i);
//}
